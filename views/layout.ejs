<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title%></title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css"
    />
    <link rel="stylesheet" href="/css/layout.css" />
    <link rel="stylesheet" href="/css/footer.css" />
    <link rel="stylesheet" href="/css/header.css" />

    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
  
    <%- style%>
  </head>
  <body>
    <%- include('_header')%> <% if(flash.success && flash.success.length > 0) {
    %>
    <h5><%= flash.success %></h5>
    <%}%> <%- body%> <%- include('_footer')%> <%- script%>
    <script>
      <% if (flash.success && flash.success.length > 0) { %>
        new Noty({
          theme: 'relax',
          text: '<%= flash.success %>',
          type: 'success',
          layout: 'topCenter',
          timeout: 1000,
          className: 'custom-notification-class' // Add your custom class here
        }).show();
      <% } %>

      <% if (flash.error && flash.error.length > 0) { %>
        new Noty({
          theme: 'relax',
          text: '<%= flash.error %>',
          type: 'error',
          layout: 'topCenter',
          timeout: 1000,
          className: 'custom-error-class' // Add your custom class here
        }).show();
      <% } %>
    </script>
  </body>
</html>
